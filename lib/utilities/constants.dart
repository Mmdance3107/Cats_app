import 'package:flutter/material.dart';

const double fontSizeValue = 18;
const nameTextStyle =
    TextStyle(fontWeight: FontWeight.bold, fontSize: fontSizeValue);
const valueTextStyle = TextStyle(fontSize: fontSizeValue);

const wikiTextStyle = TextStyle(
  fontSize: fontSizeValue,
  decoration: TextDecoration.underline,
  color: Colors.white,
);
const spaceBox = SizedBox(height: 10.0);
const radius = BorderRadius.all(Radius.circular(10));
